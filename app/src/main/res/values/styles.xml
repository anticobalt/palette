<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.Cyanea.Light.DarkActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>

    <style name="AppTheme.NoDefaultActionBar">
        <!-- Disables default action bar, allowing you to specify your own toolbar in the layout. -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.NoDefaultActionBar.ActionMode">
        <!-- Make ActionMode toolbar overlap existing toolbars -->
        <item name="windowActionModeOverlay">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="ToolbarTitle" parent="@style/TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textSize">20sp</item>
        <item name="android:textColor">?menuIconColor</item>
    </style>

</resources>
